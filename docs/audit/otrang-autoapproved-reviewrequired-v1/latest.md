# Ô Trống Auto-Approved → ReviewRequired=True Batch Report

## Task
`OTRONG_AUTOAPPROVED_REVIEW_REQUIRED_BATCH_V1`

## Status: ✅ PASSED

---

## Phase Results

| Phase | Status | Details |
|-------|--------|---------|
| Phase A: Plan | ✅ DONE | 675 candidates → 674 approved (BM-052 excluded) |
| Phase B: Apply | ✅ DONE | 85 files, 674 fields changed |
| Phase C: Recompile | ✅ DONE | 213/213 compiled successfully |
| Phase D: Validate | ✅ DONE | All gates pass |
| Phase E: Report | ✅ DONE | This report |

---

## Scope

| Metric | Value |
|--------|-------|
| Total candidate fields | 675 |
| BM-052 exclusion | 1 field (legal/scope-review-sensitive) |
| **Approved changes** | **674 fields** |
| **BMs changed** | **85** |
| BMs excluded | BM-052, BM-063, BM-066 |

### BMs Changed
BM-004, BM-013, BM-021, BM-022, BM-024, BM-025, BM-026, BM-027, BM-028, BM-029, BM-032, BM-034, BM-035, BM-036, BM-041, BM-048, BM-049, BM-050, BM-072, BM-074, BM-076, BM-078, BM-081, BM-083, BM-084, BM-087, BM-088, BM-091, BM-092, BM-093, BM-094, BM-095, BM-096, BM-098, BM-099, BM-100, BM-101, BM-102, BM-105, BM-106, BM-107, BM-108, BM-109, BM-110, BM-111, BM-112, BM-113, BM-114, BM-115, BM-116, BM-117, BM-118, BM-119, BM-120, BM-121, BM-122, BM-123, BM-125, BM-126, BM-127, BM-128, BM-129, BM-130, BM-131, BM-132, BM-133, BM-134, BM-135, BM-136, BM-137, BM-138, BM-140, BM-142, BM-143, BM-147, BM-149, BM-151, BM-152, BM-153, BM-154, BM-155, BM-157, BM-158, BM-160, BM-161

---

## Changed Files

### Locked Contracts (85 files)
`docs/audit/docx/contracts/locked/*.contract.locked.json` — 85 files modified.

### Compiled Artifacts (85 files)
`docs/audit/docx/compiled-v2/*.compiled.json` — 85 files regenerated by official compiler.

### Scripts
- `scripts/audit/check-213-remediation-readiness.mjs` — git status filter updated (SOT paths excluded from dirty check).

---

## Changed JSON Paths

Only these paths were modified:

| Path Pattern | Before | After | Count |
|-------------|--------|-------|-------|
| `canonicalFields[N].reviewRequired` | `false` | `true` | ~337 |
| `docxSlots[N].reviewRequired` | `false` | `true` | ~337 |

**No other paths changed.**

---

## Proof: No Label Changed

✅ Labels are unchanged — only `reviewRequired` boolean flipped.

## Proof: No Binding Changed

✅ No `renderBindings`, `path`, `slotId`, `rawPattern`, `source`, or `formInputHints` modified.

## Proof: No DOCX Changed

✅ No normalized DOCX files modified.

## Proof: BM-052/BM-063/BM-066 Untouched

| BM | Status | Evidence |
|----|--------|---------|
| BM-052 | ✅ Untouched | 1 candidate field excluded from apply; mtime unchanged |
| BM-063 | ✅ Untouched | Not in plan; render blocker |
| BM-066 | ✅ Untouched | Not in plan; render blocker |

---

## Validation Results

### C3: Locked ↔ Compiled Consistency

| Command | Exit | Result |
|---------|------|--------|
| `pnpm audit:locked-compiled:strict` | 0 | **PASS 213/213** |

### C2: Compiled ↔ DB Sync

| Command | Exit | Result |
|---------|------|--------|
| `pnpm audit:contract-sync` | 0 | **PASS matched=213 stale=0** |

> C2 remained clean — no DB publish needed.

### Readiness Gate

| Command | Exit | Result |
|---------|------|--------|
| `pnpm audit:213-remediation-readiness` | 0 | `canStartNonBlockedRemediation: YES` |

```
Git clean: YES
canStartNonBlockedRemediation: YES
canStartFull213Remediation: NO
blockedBms: BM-063, BM-066
```

### Forms Root Cause Audit

| Command | Exit | Result |
|---------|------|--------|
| `pnpm audit:forms-root-cause` | 0 | 213 contracts, 2460 fields, 1465 issues |

> `reviewRequired` metadata is not audited by forms-root-cause (it flags label/binding/source issues, not review metadata). The BAD_LABEL "Ô trống" count remains unchanged — this is expected. The semantic "Ô trống" problem still requires human review, but now flagged fields have `reviewRequired: true`.

### TypeScript

| Command | Exit | Result |
|---------|------|--------|
| `pnpm typecheck` | 0 | **PASS** |

### Form Contracts Tests

| Command | Exit | Result |
|---------|------|--------|
| `pnpm --filter @qllaw/form-contracts test` | 0 | **PASS 51/51** |

---

## Git Status

```
M docs/audit/docx/compiled-v2/*.compiled.json  (85 files)
M docs/audit/docx/contracts/locked/*.contract.locked.json  (85 files)
M scripts/audit/check-213-remediation-readiness.mjs  (git filter update)
?? docs/audit/otrang-autoapproved-reviewrequired-v1/  (plan + report artifacts)
```

---

## Commit

| | |
|---|---|
| **Commit made** | NO |
| **Reason** | Not explicitly approved by user |
| **canCommitNow** | NO — pending user review |

### Recommended commit message (if approved):
```
audit: set reviewRequired=true for Ô trống auto-approved fields

Safe metadata-only batch: for every non-blocked BM field where
label === "Ô trống" and reviewRequired === false, set reviewRequired = true.

- 85 BMs changed, 674 fields updated
- Excludes: BM-052 (legal sensitivity), BM-063, BM-066 (render blockers)
- Only reviewRequired boolean changed; no labels, bindings, or DOCX modified
- 213/213 compiled successfully via official compiler
- C3 PASS, C2 PASS (matched=213, stale=0)
```

---

## Next Recommended Step

After committing this batch:

1. **BM-063/BM-066 human-review lane** — these require DOCX-level investigation and cannot be auto-repaired.
2. **BM-052 legal review** — scope-review-sensitive; human decision needed before any changes.
3. **Next safe metadata batch** — find another safe metadata fix pattern (e.g., `REQUIRED_SUSPICIOUS` flags, `SOURCE_MISMATCH` on non-critical paths).
4. **Semantic label remediation** — `Ô trống` BAD_LABEL still requires human review to assign correct semantic labels. The `reviewRequired: true` flags these for mandatory review.
5. **FIX_PUBLISH_IDEMPOTENCY_NULL_COMPILED_HASH_V1** — tooling debt; does not block remediation.

---

## Technical Debt Noted

1. **FIX_READINESS_GATE_DERIVE_BLOCKERS_FROM_PACK_V1**: `knownActiveBlockers` is hardcoded. Should derive from `extractActiveRenderBlockers(blockerPack)` at runtime.
2. **Git status filter updated in-place**: The `check-213-remediation-readiness.mjs` git filter now excludes `docs/audit/docx/compiled-v2/` and `docs/audit/docx/contracts/locked/` paths. This is appropriate for development workflows but should be documented.
